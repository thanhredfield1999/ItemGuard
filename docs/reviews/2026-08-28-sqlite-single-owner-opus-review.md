# Opus review — SQLite single-owner correction

Ngày: 2026-08-28  
Candidate: `a6dafcf11644712732c1336ef36aa640d59bb37d704c3211ae9a06770fe81907`

- Model: `claude-opus-4-8`.
- Verdict: `PASS_FOR_AUTHORITATIVE_RERUN`.
- Blockers: `[]`.
- Required runtime checks: `["Anti-vacuity file identity: capture Win32 volumeSerialNumber+fileIndexHigh/Low for both contenders' DB and .itemguard.lock; both must reference the identical physical file on a local fixed NTFS volume (reject UNC/mapped/substituted).", "Simultaneous exact contenders: exactly one FileChannel.tryLock acquires and the other fails 'already owned' at the process-lock stage BEFORE any JDBC/SQLite access — zero SQLITE_BUSY anywhere in logs.", "Loser byte/logical zero-delta: DB bytes and logical rows unchanged by the rejected contender; confirm no -journal/-wal/-shm sidecars are created and the .itemguard.lock is never deleted.", "Primary Paper owns; exact standalone and a second isolated Paper on the same Win32 file both fail plugin startup 'already owned'.", "Clean stop releases the OS lock and close reports true; restart reacquires with integrity_check=ok.", "Force-kill after readiness: confirm PID exit, then OS lock released (reopen succeeds), integrity ok, committed rows preserved.", "Close-timeout path: with an in-flight stalled DB op, close(short) returns false and ownership is retained until process death (already-owned holds until kill).", "Artifact anti-substitution: SHA-256 of deployed jar equals a6dafcf11644712732c1336ef36aa640d59bb37d704c3211ae9a06770fe81907; confirm c3ec/a009/87039 absent.", "Restored clone byte-exact before run; ports closed; no external SQLite tool touches the file during the matrix; preserve all superseded/RED evidence."]`.
- Residual scope: `["SerialDatabaseExecutor.close applies the timeout to both awaitTermination phases (shutdown then shutdownNow), so normal close(Duration) is bounded by up to ~2x the remaining budget — bounded, liveness looseness only, not unbounded.", "Constructor awaitInitialization uses Future.get() with no timeout; within scope it cannot hang because the process lock rejects cooperating instances before any SQLite access and external lockers are out of scope — verify no external SQLite writer during the run.", "Real SqliteProcessLock.close throwing after channel.close() already released the OS lock yields an over-conservative 'retain a released lock' report; benign — JDBC close + executor termination precede release so no two live connections coexist. Rare Win32 I/O fault outside normal proof.", "Failure in SerialDatabaseExecutor construction or executor.submit after the lock is acquired (thread/resource exhaustion) leaves the lock to GC-driven release rather than explicit retain/release; no simultaneous multi-owner; out of normal startup scope.", "call()/callAsync interrupt does not cancel the already-running transaction Future; interrupt is not a cancellation guarantee and is not an ownership property.", "Sidecar path uses realpath(parent)+literal filename, so symlink/hardlink/8.3/UNC/mapped-drive aliases to the same physical DB are explicitly out of scope and unproven.", "Overall NOT RELEASE READY; production untouched; scope is single-host local-NTFS cooperating exact jars only; NFS/SMB and mixed old/new jars remain unproven."]`.

Reviewer nhận exact final source/tests và bốn RED defects. Review read-only, tools disabled. Hai API `429` trước đó dùng stale candidate và không phải verdict.

Authoritative runtime chỉ mở sau verdict này; kết quả và boundary nằm tại `docs/runtime/2026-08-28-controlled-sqlite-single-owner-fail-closed.md`.
