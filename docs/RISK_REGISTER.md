# ItemGuard — Risk Register

Cập nhật: 2026-09-18

| ID | Mức | Trạng thái | Evidence / tác động | Mitigation bắt buộc |
|---|---|---|---|---|
| IG-R001 | Critical | Mitigated controlled player/chest/multi-source/hopper topology/chunk-unload/block-replacement/double-chest-border + same-host cooperative SQLite single-owner | Sidecar gate `a6dafcf1…1907` giữ exactly-one owner. Hopper topology candidate `0fa1e247…2327` giữ block-container positive + minecart/virtual fail-closed. Same-tick candidate `fea0b09c…b15e` chứng minh hai block hoppers cạnh tranh một loaded double chest. Candidate `2e1fb7d8…adc5` fail-closed stale block inventory sau real pre-write chunk unload, same-location exact-byte block replacement và logical double-chest chunk-border both-unload; abort/retry/restart/restore PASS. | Multi-writer/multi-server/shared/network filesystem không supported. Hopper entity-container persistence, natural break/place/physics, hơn hai initiators/density, unopened/custom inventory/performance, offline/disconnect, external và destructive quarantine còn mở. |
| IG-R002 | Critical | Mitigated source/unit + controlled wrong-datatype PDC | Đã xóa heuristic material/name. Resolver `ABSENT/COMPLETE/CORRUPT` fail-closed. Controlled RED xác nhận `get(key, STRING)` ném trên `IntTag` trước cancellation; adapter nay kiểm datatype trước typed read và phân loại wrong type `CORRUPT`. Authoritative rerun giữ 4/4 wrong-type entities, zero exception/coalesce qua restart. | Repair identity phải là audited command riêng về sau; mọi identity-key read mới phải qua guarded service/resolver. |
| IG-R003 | Critical | Mitigated by unit | Bounded exact-slot mutator; Paper 1.21.11 contract index 0–42 đã đối chiếu. Pickup tag ground entity source. | Cần Paper journey hotbar/storage/armor/offhand. |
| IG-R004 | High | Mitigated by source/unit | Full-world entity scan và remove methods đã bị xóa; ledger chỉ giữ latest epoch. | Destructive action vẫn cấm tới khi deterministic quarantine có evidence. |
| IG-R005 | Critical | Mitigated source/integration + controlled same-host single-owner; production open | Một serial executor sở hữu một SQLite connection; sidecar `FileLock` lấy trước JDBC và giữ qua lifecycle. Tám lifecycle/ownership defects RED→GREEN; focused `36/36`, full `283/283`, repeat `10/10`, Opus 4.8 PASS. Controlled exactly-one, clean/force-kill handoff, Win32 identity, DB zero-delta và byte-exact restore PASS. | Chỉ local fixed NTFS + cooperating exact jars. Multi-server/shared/network FS, mixed old/new jars, external SQLite writer và production vẫn unsupported/not verified. |
| IG-R006 | Critical | Mitigated controlled gameplay scopes + pre-write block-container unload/replacement/double-chest-border + same-host cooperative single-owner; residual scopes open | Multi-source và SQLite evidence giữ nguyên. Hopper topology correction ngăn tracked/eligible item đi vào minecart/virtual topology không thể observation. Same-tick run giữ two-source publication. Pre-write unload, same-location exact-byte replacement và logical double-chest border both-unload đều chứng minh stale source proposal `ABORTED/SOURCE_CHANGED`, physical item giữ untagged, retry cùng source key tạo distinct `PUBLISHED`, zero unwanted delta. | Không claim minecart persistence, multi-writer/multi-server/shared FS; external deletion/tombstone, offline/disconnect, broader crash/unload stress, relocation, natural break/place/physics, hơn hai hoppers/density, unopened/custom inventory/performance và production scale còn open. |
| IG-R007 | High | Current local build+review+controlled stackable + two-player + double-chest + multi-source + same-tick double-hopper + pre-write unload/replacement/border gates verified | Candidate `2e1fb7d8…adc5`: three RED contracts close stale inventory/public bypass; focused `28/28`, full `292/292`, Opus 4.8 PASS. Real single-chest unload produced `ABORTED/SOURCE_CHANGED`, NBT untagged source, retry distinct `PUBLISHED`, independent `56/56`. Same-location `CHEST→BARREL→CHEST` cũng abort/retry distinct, verifier `199/199`. Logical double chest qua ranh giới chunk: both real unload/load events, old `9VGRD4` abort, fresh `H0FBX9` publish, stopped DB/NBT/restart/cleanup/restore và independent verifier `143/143` PASS; fallback review attempt 7 PASS vì Claude provider unavailable. | Residual: natural break/place/physics, >2 hoppers/density/performance, non-chest/custom inventories, Folia, multi-server và production. |
| IG-R008 | High | **Mitigated controlled 2026-09-16** | Reload preflight so sánh DB/task/material/hook/Discord/reclaim settings; thay đổi restart-sensitive bị từ chối trước swap và báo restart-required. Controlled Paper journey đã chạy trên candidate `082d73b3…7f2d9c`, fixture `itemguard-lite-isolated-6771cd363790`: `/reload confirm` xong identity `ASJEWP` đọc lại y nguyên, **7/7 listener vẫn đúng một đăng ký** (`handlers_that_grew` rỗng), không nhân đôi history row, `errors_during_reload` rỗng, ports đóng, children exit 0 không forced. Verdict `PASS_RELOAD`. | Reload qua hot-loader/PlugMan, reload khi đang có người chơi thao tác, và production topology vẫn chưa kiểm. |
| IG-R009 | High | Mitigated controlled | WorldGuard mặc định disabled. CompileOnly `worldguard-bukkit:7.0.16`, `softdepend`, typed `wrapPlayer/hasPermission`. Controlled WorldEdit 7.3.18 + WorldGuard 7.0.16: non-op zero publication; same physical sword after op `PUBLISHED`. Missing plugin remains fail-closed. | Production permission topology vẫn cần phê duyệt/observation; hook/query exception tiếp tục deny. |
| IG-R010 | High | Mitigated controlled fail-closed + final Opus PASS | Unsafe result tagging/double-tag path đã bị thay bằng `CraftOutputPolicy`: tagged ready/partial và eligible untagged output đều cancel; chỉ untracked untagged allow. Generic `InventoryClickEvent` từng pre-empt `CraftListener`; runtime RCA + RED regression dẫn tới subtype defer. Run `33c4beab-...`: normal/shift event-cancel `1/1`, denials2, zero consumption/output/DB delta, restart/cleanup PASS; full `232/232`, `382/382` entries identical. Final exact Opus 4.8 PASS, blockers/fixes rỗng, không cần candidate change. | Giữ fail-closed. Transactional craft issuance, smithing/anvil/stonecutter/custom plugin mutation và production deployment/verification vẫn open, cần phê duyệt riêng. |
| IG-R011 | Critical | Mitigated fail-closed / capability open | Support matrix phân biệt official `PlayerVaults` 4.4.x (`getVaultNumbers/getVault` public nhưng sync file I/O, có side effect tạo holder file, no hard bound), Modrinth `PlayerVaultsX` 1.0.x fork (thiếu enumerate/read API), zAuctionHouse V3 và V4 4.0.1.2 (public sync full-list/full-bucket, thiếu owner pagination và cross-storage read consistency). V3 có exact executable reason; mọi plugin version vẫn `UNAVAILABLE`, không giả `ABSENT`. Capability `ERROR` ưu tiên `PRESENT`; lookup exception/null thành `DENIED_ERROR/CAPABILITY_SETUP`. Exact-current-hash Opus 4.8 review PASS. Successor runtime predecessor `357aa5ce-...` deny `PLAYER_VAULTS`; claim persist `DENIED`; DB có 8/8 claims `DENIED`, no issuance. | Cần upstream bounded, side-effect-free identity query cho mọi vault/listed/purchased/expired storage với thread/read-consistency contract, timeout/error runtime, cooldown/quota và issuance transaction trước khi mở gate. |
| IG-R012 | High | Open | Description yêu cầu chặn drop/chest và chỉ trade, nhưng transaction/trade boundary chưa được định nghĩa. | Chốt UX/compatibility; không chặn chung trước khi có approved trade path và tests cho death, admin, hopper, crafting, anvil, mailbox. |
| IG-R013 | Critical | Mitigated controlled recovery scopes + same-host cooperative single-owner + hopper topology/pre-write unload/replacement/double-chest-border restart; residual recovery scopes open | Single-owner gate giữ DB zero-delta qua contention/handoff. Hopper topology và same-tick runs giữ exact identities/SHA/source attribution qua restart. Pre-write block-container unload, same-location exact-byte replacement và logical double-chest border both-unload đều abort stale proposal, giữ physical source untagged, retry distinct `PUBLISHED` và restart giữ exact tagged bytes; cleanup/restore PASS. | Không claim atomic Bukkit+DB, OS fsync, tombstone, minecart persistence, multi-writer/multi-server/shared FS hoặc toàn timing domain. Offline/disconnect, broader crash/unload stress, relocation, natural break/place/physics, hơn hai hoppers/density, unopened/custom inventory/performance/production scale vẫn `NOT VERIFIED`. |
| IG-R014 | Medium | Mitigated source/JDBC offline; runtime open | P1a service dùng PublicItemCodeGenerator/SecureRandom, config/test thống nhất 6 ký tự; typed collision retry/exhaustion/concurrent source và DB reopen covered. 364 Java tests, exact JAR/Opus5 followup PASS offline. Không rewrite legacy identities. | Natural-break/physical NBT/Paper restart còn OPEN; public code không là secret hoặc authorization. Evidence `docs/reviews/2026-09-09-p1a-generator-service-evidence.md`. |
| IG-R015 | High | Mitigated controlled command/GUI UX; production open | UX candidate `06cf01c7…b037` full Java `266/266`, exact Opus 4.8 PASS. Sau BotChecker P0/P1 TDD/full gate, authoritative Paper `1.21.11-131` rerun PASS: member `24/24`; staff `38/38`, exact `29` items/`28+1`, Unicode output, Adventure title/name/lore, filter/history/detail/back/final close. Evidence archive `b64914fe…9a00`; historical harness/oracle RED vẫn giữ audit chain. | Không suy controlled UX PASS thành release/production approval. Rapid quit/reopen ordering, production topology/scale và các anti-dupe/persistence/external/issuance gates khác vẫn cần evidence/phê duyệt riêng. |
| IG-R016 | High | Open — factual client-load/Paper/Bukkit break path verified; fresh-identity seam inconclusive; scaffold incidents closed fail-closed, Attempt 14 execution not established | Attempt 12 dùng candidate `2e1fb7d8…adc5`, bundle 18 manifest `59923469…fe611`, independent PASS chỉ cho consumed namespace. Exact `player_loaded` `0x2b`, factual `PlayerClientLoadedWorldEvent(timeout=false,preArm=0)`, JDI `CLIENT_LOADED_PASS→HANDLE_BLOCK_BREAK_ACTION_ENTRY→GAME_MODE_RANGE_PASS`; uncancelled Bukkit interact/damage/break, exact drop và content entity-add. Terminal `fresh-identity-timeout` sau 59.889s; thiếu entity terminal/listener/request/publication/stopped DB-NBT evidence nên product `NOT_ISSUED`. Attempt 13 fail trước Paper do files-only owner-seal `StopIteration`; successor regression offline PASS. Bundle 34 sau Attempt 14 thiếu đúng 45 payload manifest và Bitdefender metadata khớp 45/45 path+SHA; current activation/trigger/Paper log/verdict absent và post-state clone/DB/seal exact, nhưng không có sealed contemporaneous journal để loại trừ hậu-xóa hoặc transient launch, nên execution giữ `NOT_ESTABLISHED`, process causality inconclusive. Incident: `docs/incidents/2026-09-01-natural-break-evidence-scaffold-preflight-and-bundle-integrity.md`. | Không rerun attempt 12/13/14 hoặc reuse receipt/bundle 18/31/34; không restore/whitelist quarantine hay bypass antivirus. Successor phải có explicit antivirus-safe design, sealed contemporaneous execution journal độc lập với executable payload, TDD host interruption/quarantine fail-closed, receipt ItemListener invocation, deferred stable entity, request outcome, publication state/detail, bounded physical entity snapshots, stopped DB/NBT và restore-safe deploy; seal/review namespace mới trước runtime. |

## Quy tắc đóng risk

### 2026-09-09 — C3 retained-observation browser offline, custody/runtime OPEN

- Fresh 378 Java tests/52 catalog, Opus5 R2/followups và exact 218-class JAR verified; evidence `docs/reviews/2026-09-09-retained-observations-evidence.md`. Cache read bounded, errors không giả empty/ABSENT, same-epoch và cross-epoch đều không chứng minh bản sao đồng thời. Profile nguồn tracked_items có nhãn riêng.
- Không lịch sử chuyển tay bền vững/current holder/full-system completeness/HavenBags adapter. Older epochs bị prune, completed chỉ bounded online/open-block scan. Raw grouping key/lore dài và shared 1s gate còn UX C4; current schema NULL rejection có test, hypothetical nullable legacy schema không tự được hỗ trợ/migrate.
- Không Paper/client/visual/deploy; runtime UX gate cũ không chứng nhận GUI mới. Không thay schema/writer/scan hay cấp quyền reclaim/quarantine.

### 2026-09-09 — C1 mitigation offline, live/search-scale vẫn OPEN

- Product schema v8 composite history index + atomic stamp/rollback/conflict validation đã có 9 migration tests, final full 351 PASS. Three synthetic >1M histories dùng indexed exact identity/tie order; 60 queries PASS. Không còn dùng kết luận schema v7 scan để đại diện candidate mới.
- Cursor/filter chỉ commit khi load thành công; retry exact request và return-to-committed-page qua controller + real admission-gate tests. Opus5 implementation + bounded followup PASS_FOR_OFFLINE_CANDIDATE, artifact/hash verified; Paper/visual chưa verified.
- Missing contains-text search trên 100k records vẫn SQLITE_INTERRUPT. Không tăng budget, không hứa general-search SLO; cần design riêng nếu đổi cấu trúc tìm kiếm. Migration cap 100k progress checks/30s sampled không là hard I/O deadline; disk-full/power-loss/live backup-restore/larger DB envelope chưa test. v7 JAR không mở v8; không downgrade stamp để rollback.
- Evidence: `docs/reviews/2026-09-09-catalog-c1-evidence.md`. Không production action, không mở các gate destructive.

### 2026-09-08 — Catalog pre-live risks OPEN

- 30 catalog tests / full 334 PASS + Opus5 bounded offline review không thay thế controlled GUI cũ bằng runtime proof cho GUI catalog mới.
- Existing history schema trên 1M synthetic rows: SCAN + sort, SQLITE_INTERRUPT; scratch-only index experiment exact 100 results. Product index/migration design và workload validation vẫn OPEN; không bỏ budget để lấy xanh.
- Shared DB one-inflight + 1s admission chống queue flood nhưng fast/multi-admin paging có thể trả lỗi. Sampled VM/time cap không phải I/O deadline. Paper chat/drag/creative/disable/inter-plugin/visual chưa verified.
- Provider/HavenBags/custody/full-system coverage chưa hỗ trợ; owner/actor không tự là current holder/ownership. Evidence: `docs/reviews/2026-09-08-catalog-implementation-evidence.md`.

- `Mitigated by unit test` không đồng nghĩa runtime verified.
- Critical/High liên quan anti-dupe, persistence, async hoặc integration cần review độc lập.
- Destructive mode chỉ được enable sau controlled Paper journey + restart evidence; production cần phê duyệt riêng.

### 2026-09-16 — Independent Opus 5 review: 3 Critical confirmed và đã sửa, phần còn lại mở

Review độc lập toàn repo (read-only, không sửa). Ba defect Critical dưới đây **không cổng nào bắt
được**: 806 test xanh trên đúng bộ code đó. Chúng thuộc lớp "code đọc vẫn đúng nhưng nói sai với
người chơi". Mỗi mục đã tự xác minh lại bằng source trước khi sửa.

| ID | Mức | Trạng thái | Nội dung |
|---|---|---|---|
| IG-R017 | Critical | **Mitigated 2026-09-16** (C2) | Cache readiness trong bộ nhớ + `isIdentityReady` chỉ đọc cache ⇒ item đã tag trong ender chest/rương xe mỏ bị cancel click **vĩnh viễn** và im lặng sau restart. Sửa: reconcile khi tiếp xúc trong `isIdentityReady`, source key `unresolvedHolder` nằm ngoài namespace publication nên chỉ xác nhận identity đã có, không cấp mới. RED bằng assertion + mutation check. |
| IG-R018 | Critical | **Mitigated 2026-09-16** (C3) | `reconcileTagPublication` trả false vĩnh viễn khi thiếu row canonical ⇒ pickup bị cancel ⇒ item despawn ⇒ **mất thật**. Sửa: adopt **có điều kiện** — chỉ khi journal chưa từng có code/uuid đó; row ghi `last_action='ADOPTED'` + history `ADOPTED`. Identity journal đã biết vẫn fail-closed. |
| IG-R019 | Critical | **Mitigated 2026-09-16** (C1) | Epoch trải nhiều tick (lượt quét túi 1 tick, sweep container tới ~60s cùng epoch) ⇒ mọi cú chuyển đồ trong cửa sổ đó thành `CONFIRMED` dupe. Sửa theo quyết định Thanh: yêu cầu ≥2 vị trí ở **2 epoch liên tiếp**. Giữ phát hiện túi↔rương; chậm 1 chu kỳ (30s). |
| IG-R020 | High | **Mitigated 2026-09-17** (một phần còn hở, ghi rõ bên dưới) | H5 — đã sửa: `DEFAULT_MESSAGES` nay là văn bản EN ghim key+value với `messages_en.yml` (`MessageManagerFallbackTest`); `MessageLanguagePolicy` buộc LITE luôn English (VI vẫn là tính năng FULL, có test); `lite/config.yml` bỏ `# en or vi`; **cả hai cổng đóng gói** (`package_lite.py` + verifier độc lập) nay dùng **allowlist**: chỉ `plugin.yml`/`config.yml`/`messages_en.yml` được ở gốc jar, entry không-phải-class khác phải thuộc thư viện đã shade — nên `lang/vi.yml` hay `help_zh.txt` thêm sau sẽ bị chặn. Self-test verifier 12/12 (4 ca mới khẳng định luật thật sự bắn), verifier trên jar thật `ARTIFACT_CONSISTENT_OFFLINE_ONLY`, 100/100 tooling contract. | **Còn hở:** (a) chuỗi VI **không dấu** trong `.class` là vô hình với mọi phép quét codepoint — hàng rào thật cho lớp lỗi đó là parity test Java, không phải quét byte (quét byte chỉ tạo cảm giác có coverage); (b) chưa có phép thử end-to-end bằng cách chèn file lạ vào jar thật rồi chạy verifier — hiện chỉ chứng minh hàm luật trả finding, còn đường finding → REJECT là qua đọc wiring. |
| IG-R021 | High | **Mitigated 2026-09-17** with mutation-proven tests (H1) | Yêu cầu lọc chat không có hạn: bấm Filter rồi bỏ đi thì tin nhắn kế tiếp — có khi hàng giờ sau, giữa cuộc trò chuyện — bị **cancel** và hiểu thành truy vấn lọc; người chơi thấy tin nhắn biến mất. Sửa: TTL 30 giây (`FILTER_INPUT_TTL_MS`), hàm thuần `expired(...)`, người chơi được nói trước cửa sổ và được báo khi yêu cầu hết hạn — và tin nhắn **không** bị nuốt nữa. Gỡ hàng rào (mutation) làm 2/3 test đỏ. | Chưa có test runtime: đường chat của Bukkit (`AsyncPlayerChatEvent`) vẫn chỉ được kiểm ở tầng hàm thuần. |
| IG-R022 | High | **Partially mitigated 2026-09-18** | P0 async slice chuyển `CheckCommand` sang `getItemAsync` + `getHistoryCountAsync`, scheduled cleanup sang `deleteOldHistoryAsync`, và `InventoryScanTask` sang async persisted-epoch initialization; controlled Paper 1.21.11 MySQL single-server restart, two-server shared-MySQL lifecycle và `/ig migrate` dry-run/confirm/refusal/restart journey đã pass trên Premium JAR `8f39da47…392fc`. `HistoryCommand`, `SearchCommand`, `StatsCommand`, `LiteCommand`, `GUIListener` đã dùng async paths. Còn lại: các sync API công khai (`DatabaseManager`/`ItemTrackingService`), legacy/external callers, full player/gameplay journey và GUI in-flight boundary. Không claim main-thread safety toàn plugin. Evidence: `run/premium-paper-migration-20260918-192842.json`. | Audit caller inventory và chuyển từng Paper path còn sync sang async/bounded operation; hoàn tất gameplay/GUI journey trước release. |
| IG-R023 | High | **Mitigated 2026-09-17** với mutation-proven tests (H3 + H4) | H3: `ContainerListener.cooldowns` key bằng `holder.toString()` — rương in toạ độ nên tình cờ đúng, holder rương xe mỏ/unknown in identity hash **đổi mỗi snapshot** ⇒ cooldown không bao giờ khớp và map lớn vô hạn. Sửa: key ổn định (`BLOCK:<world-uuid>:x:y:z`, `ENTITY:<uuid>`, còn lại bounded), prune khi ghi + trần 4.096 entry. H4: `detection-cooldown-ms` 5 giây so với chu kỳ quét 30 giây nên **không thể** chặn lần báo lặp nào ⇒ staff bị báo lại mỗi 30 giây mãi mãi; nay cửa sổ bị sàn bằng một chu kỳ quét, default 5000 → 300000 (có test nạp chính `config.yml` ship ra). Mutation: H3 làm 1/5 test đỏ, H4 làm 3/5 test đỏ. | Nửa sau của H4 **chưa làm và đang chờ quyết định sản phẩm**: bảng `duplicate_findings` phình không retention, mà LITE hứa "không bao giờ dọn lịch sử" nên xoá finding là phá bằng chứng điều tra. |
| IG-R024 | High | Open | H6 — hopper fail-closed làm ngưng hệ thống phân loại của người chơi, không log, không cảnh báo. |
| IG-R025 | High | Open | H7/M15/M13/M12 — Discord webhook, `VaultHook`, `handleFilterInput`, `general.enabled`, `logging.*`, `database.pool.*` là code/config **chết**; config hứa tính năng không tồn tại. |
| IG-R026 | High | **Mitigated for Premium shared-runtime path 2026-09-18** | H8 — Premium runtime uses explicit `MysqlDataSource` behind HikariCP; Connector/J/Hikari/SLF4J are shaded and relocated. Controlled Paper single-server restart, two independent Paper servers sharing one MySQL schema, and Paper `/ig migrate` dry-run/confirm/refusal/restart all pass with the exact Premium JAR; provider errors are empty and cleanup is verified. SQLite remains the separate LITE path; no claim that this changes SQLite driver ownership. | Keep the SQLite/LITE boundary unchanged; do not publish Premium until reliability/failure gates and full player/gameplay evidence are verified. |
| IG-R027 | Medium | Open | M1/M2/M8/M9/M11/M16 — tên Material sai trong `force-track-materials` (16 shulker box), key config chết, `auto-cleanup.enabled` không được đọc (FULL xoá lịch sử >30 ngày — mâu thuẫn định vị "không dọn dữ liệu"), filtering áp toàn resources, `messages.yml` không bao giờ được ghi ra, `argLine` hardcode Mockito. |
| IG-R028 | Medium | **Mitigated for Premium shared-MySQL owner 2026-09-18; caller boundary remains under IG-R022** | NEW-10 — Premium `MySqlConnectionOwner` does not use the SQLite process lock; Hikari/DataSource ownership and transaction-scoped identity locking were exercised by two independent Paper JVMs sharing one MySQL schema. The controlled probe recorded `server-1=2`, `server-2=1` observations and a named `SEEN_ON_MULTIPLE_SERVERS` assessment without duplicate/copy language. SQLite's local-owner limitation remains intentional. Evidence: `run/premium-two-paper-mysql-20260918-190633.json`. |

Báo cáo đầy đủ: `docs/reviews/2026-09-16-opus5-independent-audit.md` (raw review, C1–C3/H1–H8/M1–M16
kèm file:line). Ba Critical đã sửa có test hồi quy; các mục còn lại là **finding chưa được tự xác
minh**, không phải kết luận đã kiểm chứng.

Chưa chạy lại sau khi sửa: `scripts/test_contracts.py` (100 contract LITE) và các cổng artifact.
`CURRENT_STATE.md` ghi rõ candidate LITE `a952d161` không còn đại diện cho code hiện tại.

